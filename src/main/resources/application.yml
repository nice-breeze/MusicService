
spring:
  application:
    name: music-service
  flyway:
    locations: classpath:/db/migration
    schemas: music
    enabled: true
  datasource:
    driverClassName: org.postgresql.Driver
    url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/pomodoro?currentSchema=music}
    username: ${DATASOURCE_USERNAME:my_app_user}
    password: ${DATASOURCE_PASSWORD:nicebreeze}

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
  show-actuator: true

logging:
  level:
    root: INFO
    org.springframework.data.jpa.repository: INFO